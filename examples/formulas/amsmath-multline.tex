% The first line is left-aligned, the last line right-aligned, and all other lines in between are centered
\begin{multline}
  \sum = a + b + c + d + e   \\
         + f + g + h + i + j \\
         + k + l + m + n 
\end{multline}